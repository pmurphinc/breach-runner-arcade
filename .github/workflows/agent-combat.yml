name: Agent Combat Feedback Pass

on:
  push:
    branches: [codex/combat-feedback-polish]

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Apply combat patch
        run: python .github/agent-combat.py
      - name: Install dependencies
        run: npm ci
      - name: Focused regression
        run: node --test tests/combat-feedback.test.mjs
      - name: Full test gate
        run: npm test
      - name: Lint gate
        run: npm run lint
      - name: Commit verified pass
        run: |
          git config user.name "OpenAI Codex"
          git config user.email "noreply@openai.com"
          git add -A
          git commit -m "Add combat haptics cannon feedback and EMP clarity"
          git push origin HEAD:codex/combat-feedback-polish
